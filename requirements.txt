# --- Geospatial data sourcing ---
earthengine-api>=1.1        # Google Earth Engine Python client
geemap>=0.32                # GEE <-> Python/folium helpers, interactive maps
geopandas>=0.14
shapely>=2.0
rasterio>=1.3               # read/write GeoTIFFs
gdal>=3.6                   # (often easier via conda; see README)

# --- Arrays & data ---
numpy>=1.26
pandas>=2.0
scipy>=1.12

# --- Deep learning (U-Net) ---
# Installed separately on Colab where the CUDA build is provided.
torch>=2.2
torchvision>=0.17
segmentation-models-pytorch>=0.3   # battle-tested U-Net implementations
albumentations>=1.4                 # image+mask augmentation

# --- Economic analysis ---
statsmodels>=0.14          # lagged regression, OLS diagnostics

# --- Viz & notebooks ---
matplotlib>=3.8
plotly>=5.20
jupyter
tqdm>=4.66
